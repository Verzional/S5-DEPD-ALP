import 'package:flutter/material.dart';

class UCHubColors {
  // 🔵 Blue Gradient (Find a Great Hire / More About the Job)
  static const Color primaryStart = Color(0xFF46A9FE); 
  static const Color primaryEnd = Color(0xFF30CEF2);

  // 🟠 Orange → Pink Gradient (Register, CTA buttons if needed)
  static const Color secondaryStart = Color(0xFFFF8904);
  static const Color secondaryEnd = Color(0xFFF6339A);

  // Backgrounds
  static const Color contentBackground = Color(0xFFF3FCFF);
  static const Color footerBackground = Color(0xFF333333);

  // Text Colors
  static const Color textDark = Color(0xFF212121);
  static const Color textGrey = Color(0xFF616161);

  // Blue Accent Title
  static const Color accentBlueText = Color(0xFF4A90E2);

  // Light Blue Borders
  static const Color pillBackground = Color(0xFFE9F6FF);
  static const Color borderStart = Color(0xFFB5E0FF);
  static const Color borderEnd = Color(0xFF7AAFFF);
}
